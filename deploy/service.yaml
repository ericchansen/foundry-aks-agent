apiVersion: v1
kind: Service
metadata:
  name: boring-agent
  namespace: boring-agent
  labels:
    app.kubernetes.io/name: boring-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: boring-agent
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
